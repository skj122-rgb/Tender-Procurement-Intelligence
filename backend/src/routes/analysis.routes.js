const express = require('express');

const router = express.Router();

// Analysis routes are mounted under tender routes as per requirements
// This file is kept intentionally empty.

module.exports = router;
